import { CreateDirective } from './create.directive';

describe('CreateDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateDirective();
    expect(directive).toBeTruthy();
  });
});
